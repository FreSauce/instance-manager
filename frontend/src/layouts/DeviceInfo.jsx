import React from 'react'
import { Box } from '@mantine/core';
import { motion } from 'framer-motion';

const DeviceInfo = ({ device }) => {
	return (
		<motion.div animate>
			<div style={{ backgroundColor: '#191c24', minHeight: 'inherit' }}>
				hehe
			</div>
		</motion.div>
	)
}

export default DeviceInfo